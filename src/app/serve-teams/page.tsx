import SectionPage from '@/components/SectionPage'

export default function ServeTeamsPage() {
  return (
    <SectionPage
      emoji="🙌"
      title="Serve Teams"
      description="Find your place to serve."
      comingSoonMessage="Browse FIC's serve teams, set your availability, see team announcements, and connect with your team leader. Each ministry will have its own space to coordinate."
    />
  )
}
